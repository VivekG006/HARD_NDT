# ============================================================
# HARD NDT — Python Dependencies
# Target runtime: Python 3.11.9 (strictly)
#
# System requirement: ffmpeg + ffprobe must be on PATH
#   Windows: https://www.gyan.dev/ffmpeg/builds/ (full build)
#   Linux:   sudo apt install ffmpeg
# ============================================================

# --- Core numerics ---
numpy==2.4.2
scipy==1.17.1

# --- Audio DSP ---
librosa==0.11.0
soundfile==0.13.1
audioread==3.1.0
soxr==1.1.0


# librosa internals
lazy-loader==0.5
msgpack==1.1.2
pooch==1.9.0
decorator==5.2.1
numba==0.65.1
llvmlite==0.47.0
joblib==1.5.3

# --- Computer Vision ---
opencv-python==4.13.0.92

# --- 3D Rendering (PyVista + VTK) ---
pyvista==0.45.2
vtk==9.4.2
Pillow==12.1.1

# --- Visualization (matplotlib fallback) ---
matplotlib==3.10.8
contourpy==1.3.3
cycler==0.12.1
fonttools==4.61.1
kiwisolver==1.4.9
pyparsing==3.3.2

# --- Configuration ---
PyYAML==6.0.3

# --- Shared infrastructure ---
packaging==26.0
platformdirs==4.9.6
python-dateutil==2.9.0.post0
six==1.17.0
threadpoolctl==3.6.0
typing_extensions==4.15.0

# --- Network / stdlib helpers ---
certifi==2026.4.22
charset-normalizer==3.4.7
idna==3.13
requests==2.33.1
urllib3==2.6.3

# --- C / FFI ---
# IMPORTANT: cffi 2.0.0 dropped pre-built wheels for Python 3.11 on Windows.
cffi==1.17.1
pycparser==3.0

# --- Astronomy (pulled in transitively by pooch on some builds) ---
astropy==7.2.0
astropy-iers-data==0.2026.2.23.0.48.33
pyerfa==2.0.1.5

# --- scikit-learn (used indirectly by librosa onset/beat modules) ---
scikit-learn==1.8.0
